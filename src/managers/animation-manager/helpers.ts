export const ANIMATIONS_ASSETS_PATH = 'animations/';

export const getAnimationAssetPath = (path: string) => {
  return ANIMATIONS_ASSETS_PATH + path;
};

export const getImagePath = (path: string) => {
  return 'assets/' + path;
};
